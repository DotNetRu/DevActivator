import { PatternErrorMessagePipe } from './pattern-error-message.pipe';

describe('PatternErrorMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new PatternErrorMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
