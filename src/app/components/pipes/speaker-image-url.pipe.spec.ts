import { SpeakerImageUrlPipe } from './speaker-image-url.pipe';

describe('SpeakerImageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new SpeakerImageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
